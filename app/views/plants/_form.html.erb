<%= simple_form_for @plant do |p| %>
  <%= p.input :name %>
  <%= p.input :plant_type %>
  <%= p.input :care_level, as: :numeric %>
  <%= p.input :location %>
  <%= p.input :description, as: :text, :input_html => { :rows => 5 } %>
  <%= p.submit %>
<% end %>
